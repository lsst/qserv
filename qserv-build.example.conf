# WARNING : these variables mustn't be changed once the install process is started
[DEFAULT]
version = qserv-dev
base_dir = /opt/%(version)s
log_dir = %(base_dir)s/var/log

[qserv]
# Qserv rpc service port is 7080 but is hard-coded

# Tree possibles values :
# mono
# master
# worker
node_type=mono

# Qserv master DNS name
master=qserv-master.in2p3.fr

# Geometry file will be downloaded by default in git master branch
# but a source directory may be specified 
# it could be retrieved for exemple with : git clone git://dev.lsstcorp.org/LSST/DMS/geom
# geometry_src_dir=/home/user/geom

# geometry_url=

[xrootd]
cmsd_manager_port=4040
xrootd_port=1094

[mysql_proxy]

port=4040

[mysqld]

port=13306

# Be careful, special characters (',%,",...) may cause error, 
# use %% instead of %
pass=changeme

data_dir=data_dir=%(base_dir)s/var/lib/mysql

[lsst]

# Where to download LSST data
# Example: PT1.1 data should be in $(data_dir)/pt11/
data_dir=/data/lsst

# Do not edit, unless you know what you are doing
[dependencies]

base_url=http://www.slac.stanford.edu/exp/lsst/qserv/download/current
zope_url=%(base_url)s/zope.interface-3.8.0.tar.gz
mysql_python_url=%(base_url)s/MySQL-python-1.2.3.tar.gz
twisted_url=%(base_url)s/Twisted-12.0.0.tar.bz2
expat_url=%(base_url)s/expat-2.0.1.tar.gz
libevent_url=%(base_url)s/libevent-2.0.16-stable.tar.gz
lua_url=%(base_url)s/lua-5.1.4.tar.gz
lua_xlmrpc_url=%(base_url)s/lua-xmlrpc-v1.2.1-2.tar.gz
luaexpat_url=%(base_url)s/luaexpat-1.1.tar.gz
luasocket_url=%(base_url)s/luasocket-2.0.2.tar.gz
mysql_url=%(base_url)s/mysql-5.1.61.tar.gz
mysql_proxy_url=%(base_url)s/mysql-proxy-0.8.2.tar.gz
protobuf_url=%(base_url)s/protobuf-2.4.1.tar.gz
scisql_url=%(base_url)s/scisql-0.3.tar.bz2
virtualenv_url=%(base_url)s/virtualenv-1.7.tar.gz
xrootd_url=%(base_url)s/xrootd.tar.gz
# scons_url=%(base_url)s/scons-2.1.0.tar.gz


