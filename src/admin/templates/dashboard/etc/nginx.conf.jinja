daemon off;

events {}

http {
    server {
        listen 0.0.0.0:{{ dashboard.port }};
        server_name  lsst-qserv-master02;

        location / {
            root   {{ dashboard.html }};
            index  index.html index.htm;
        }
    }
}
