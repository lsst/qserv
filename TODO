- add swigged target to build alias (run scons install to see)
- manage geometry.py
- package new Qserv version
- reduce and comment client configuration file, use templates in it
- manage default for server configuration file ?
- ubuntu : porting SL6 /etc/rc.d/init.d/functions to ubuntu ? use LSB
- state.py : where to save state ? print it to sdtout ?
- add ssh://git@dev.lsstcorp.org/personal/fjammes/eups-qserv distribution
  scripts in qserv/eupspkg/distribute
- in qserv-env.sh : avoid to add PYTHONPATH to system numpy : manage with eups (when doing eups declare python system -m tablefile)?
- download eups tarball on distserver instead of git clone
- retrieve logger.py
